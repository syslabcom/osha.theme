/*
  <dtml-with base_properties> (do not remove this)
  <dtml-call "REQUEST.set('portal_url', portal_url())"> (not this either)
*/

#portal-header {
    background:#E6E8ED url(<dtml-var "restrictedTraverse('@@oshaview').get_subsite_property('logo_url')">) no-repeat scroll 100% 50%;
border-bottom:1px solid #AAB1C5;
}

/* </dtml-with> */

